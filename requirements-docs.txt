# Tooling for producing the submission documents, kept separate from
# requirements.txt so the analysis environment stays exactly as pinned for the
# run. Nothing under src/atlas or tests/ imports these; `make test` does not
# need them.
#
#   reportlab  typesets cover_letter_GR.md -> PDF (tools_cover_letter_pdf.py)
#   pypdf      reads the result back to verify pagination and styling

reportlab==5.0.0
pypdf==6.15.0
