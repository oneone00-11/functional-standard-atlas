# GPU environment — created ON the rented pod / cluster (see cluster/RUNBOOK_GPU.md).
# After the run, `pip freeze` the env and replace these with the exact
# versions used (AGENTS.md rule 3); the scored output + this file + NOTES.md
# must together be sufficient to reproduce.
# Install:
#   conda create -n nt python=3.12 -y && conda activate nt
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
#   pip install transformers pyfaidx pandas pyarrow tqdm
torch        # CUDA build (cu128 wheel), exact version pinned post-run
transformers # exact version pinned post-run
pyfaidx
pandas
pyarrow
tqdm
