# Direct dependencies, pinned to the versions this analysis was run with.
# The complete resolved environment is in requirements.lock.txt.

requests==2.32.5
pandas==3.0.5
numpy==2.5.1
pyyaml==6.0.3
pytest==8.4.2
pyarrow==21.0.0
matplotlib==3.11.1
scipy==1.18.0
python-docx==1.2.0
