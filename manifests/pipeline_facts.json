{
 "note": "Quantities the manuscript cites that are derived from the pipeline but appear inside no results/ file. Regenerate with `make facts`. atlas.check_manuscript_numbers compares the manuscript's stated guardrail count against guardrail_tests_passing_from_archive rather than looking it up in the value pool: both 102 and 118 -- the counts this manuscript carried before -- match exactly one unrelated pipeline value each, so a stale count would be reported as verified. How many tests PASS depends on the checkout (a release archive ships data/ and results/, a git archive does not), so the passing count must be measured from the release at release time. It also depends on whether the machine running them can reach the network: one test verifies mapped reference bases against the Ensembl REST API and skips without it, so the archive figure is the offline one -- what a reader without network access reproduces -- and archive_measured_for_version records the reachable figure alongside it so neither can be quoted without the other.",
 "guardrail_tests_collected": 202,
 "guardrail_tests_passing": 201,
 "guardrail_tests_skipped_without_denylist": 1,
 "tests_covering_the_analyses_introduced_here": 43,
 "guardrail_tests_passing_from_archive": 178,
 "guardrail_tests_skipped_from_archive": 24,
 "archive_measured_for_version": "the v2.4.4 release archive, measured from a clean extract with HOME pointed at an empty directory: 178 passed, 24 skipped, 202 collected. Twenty-three of those skips are the same on any machine -- they need something a reader does not have (a git checkout, the author's submission directory, or the hygiene denylist, which is deliberately not archived). The twenty-fourth is not: tests/test_mapping.py checks the mapped reference bases against the Ensembl REST API and skips when that service is unreachable, so with the network available the same extract runs 179 and skips 23. This entry previously said the count 'does not depend on whose machine runs it'. That was false for exactly that one test, and it is why the manuscript stated a count a reader without network access could not reproduce. Both figures measured on 2026-09-10 from the deposited v2.4.4 archive at 10.5281/zenodo.22671905, the offline figure twice during a real Ensembl outage and once more with the API deliberately unreachable. v2.4.4 changes a manifest note and a docstring only, so the collected count is unchanged from v2.4.3",
 "guardrail_tests_passing_from_bare_clone": 130,
 "guardrail_tests_skipped_from_bare_clone": 72,
 "bare_clone_measured_for": "a fresh clone of main, independent virtualenv on Python 3.12.13, HOME pointed at an empty directory: 130 passed, 72 skipped; results/, data/ and the package absent. Measured on 2026-09-09 at a662470, the first commit past the v2.4.3 deposit",
 "release_version": "2.4.4"
}
